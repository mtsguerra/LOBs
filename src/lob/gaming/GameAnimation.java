package lob.gaming;

public abstract class GameAnimation implements Gameplay {
    // Esta será a classe base para todos os jogos (Cannon, Golf, etc.)
    // Como é abstrata, não precisamos de implementar já os métodos todos.
}