package lob.physics.shapes;

import lob.physics.engine.PhysicsWorld;

public interface FrameShower {
    // Para já fica vazia só para satisfazer o compilador.
    // Mais tarde, se o teste exigir métodos (como draw() ou show()), nós adicionamos.
    void show(PhysicsWorld world);
}